<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13 (API 33) y superiores exigen pedir este permiso en
         tiempo de ejecución. Sin declararlo aquí, `requestPermission()`
         devuelve "denegado" sin llegar a enseñar el diálogo — y no da
         ningún aviso de que falta. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="Secret Gift"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links: sin este bloque, Android no tiene ningún motivo
                 para ofrecer esta app al abrir un enlace de secretgift.app
                 (por QR o compartido) — se lo entrega directamente al
                 navegador, aunque la app esté instalada. `autoVerify`
                 dispara la verificación de Digital Asset Links contra
                 https://secretgift.app/.well-known/assetlinks.json (que
                 genera Firebase Hosting solo, a partir de las huellas de
                 firma registradas en el proyecto); sin verificar, Android
                 trata el enlace como "ambiguo" y sigue preguntando qué app
                 usar en vez de abrir esta directamente.

                 Los enlaces de invitación son la RAÍZ del sitio con
                 parámetros de consulta, p. ej.
                 https://secretgift.app/?codigo=XXXX-YYYY[&reemplazo=...].
                 Un intent-filter no puede filtrar por query string —solo
                 por esquema, host y ruta—, así que el `<data>` de abajo
                 fija `android:path="/"` para quedarse solo con la raíz
                 exacta (que es donde vive `?codigo=`) y NO capturar el
                 resto del dominio: en particular deja fuera `/apk`, el
                 atajo de descarga directa del APK de prueba, que tiene que
                 seguir yendo al navegador para poder descargarse. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="secretgift.app"
                    android:path="/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
