name: santa_secreto
description: "Regalo Secreto — Amigo Secreto y Santa Secreto para tu grupo."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.1.1
  firebase_messaging: ^16.0.0
  http: ^1.6.0
  shared_preferences: ^2.5.5
  share_plus: ^13.3.0
  qr_flutter: ^4.1.0
  google_fonts: ^8.2.1
  # Avatares. Redimensiona en el propio dispositivo antes de entregarnos
  # la imagen (maxWidth/maxHeight), así nunca sube una foto de 8MB.
  image_picker: ^1.2.0
  # Solo se usa en web (recarga_pagina_web.dart), para sessionStorage y
  # recargar la página. Ya llegaba de forma transitiva vía otros paquetes,
  # pero se declara directa porque el código de la app la importa por su
  # cuenta.
  web: ^1.1.0
  # Enlaces de invitación en Android (`?codigo=...`): entrega el Intent de
  # arranque (app cerrada) y el flujo de enlaces mientras la app ya vive
  # (app abierta). En web no se usa — ahí sigue sirviendo `Uri.base`, que
  # ya funcionaba y no dependía de ningún paquete.
  app_links: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Ya llegaba de forma transitiva vía `shared_preferences`, pero se
  # declara directa porque `test/almacen_local_test.dart` la importa por
  # su cuenta: sustituye `SharedPreferencesStorePlatform.instance` por un
  # fake que revienta al leer, para poder probar de verdad el `try/catch`
  # contra un almacén roto (con `setMockInitialValues` no se puede: ese
  # nunca lanza).
  shared_preferences_platform_interface: ^2.4.2

  # Genera los iconos de Android (normales + adaptativo) a partir de
  # assets/logo.png. Es solo una herramienta de build: no debe acabar en
  # la app publicada, de ahí que vaya en dev_dependencies.
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Genera AppLocalizations a partir de los .arb de lib/l10n (ver l10n.yaml).
  generate: true

  assets:
    - assets/logo.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Configuración de flutter_launcher_icons: solo Android (iOS/macOS/Windows
# no se tocan). Genera el icono adaptativo (mipmap-anydpi-v26, con capa de
# fondo y de figura) y también los ic_launcher.png sueltos para Android
# antiguo, ambos desde assets/logo.png.
#
# El fondo #FBF6EE es el mismo crema que ya usa web/icons/Icon-maskable-512.png
# (medido de ese fichero, no aproximado).
#
# adaptive_icon_foreground_inset al 20%: el lienzo adaptativo es de 108dp
# pero el launcher solo garantiza el círculo interior de 66dp; el resto se
# recorta. assets/logo.png llena casi todo su lienzo (el dibujo ocupa ~81-90%
# del PNG de 512px), así que sin encogerlo el launcher le cortaría bordes.
# Un inset del 20% por lado deja la figura final al ~60% del lienzo, la misma
# proporción que ya tiene Icon-maskable-512.png respecto a su propio lienzo
# (esa referencia se resolvió por prueba y no se recalcula aquí: se mide con
# el script de comprobación, no se aproxima a ojo).
flutter_launcher_icons:
  android: true
  image_path: "assets/logo.png"
  adaptive_icon_background: "#FBF6EE"
  adaptive_icon_foreground: "assets/logo.png"
  adaptive_icon_foreground_inset: 20
